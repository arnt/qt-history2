#ifndef OUTPUTREVISION_H
#define OUTPUTREVISION_H

// if the output revision changes, you MUST change it in qmetaobject.h too
enum { mocOutputRevision = 45 };          // moc format output revision

#endif
