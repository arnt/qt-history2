To install the precompiled Qt library and the binary version of moc cd to
your qt directory (or, for example, /usr) and type:

	tar xvf lib.tar

This will put the moc into the bin directory and the library into the lib
directory
