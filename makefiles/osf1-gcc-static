diff -uwr template/Makefile osf1-gcc-static/Makefile
--- template/Makefile	Thu Aug 22 20:31:11 1996
+++ osf1-gcc-static/Makefile	Fri Aug 23 07:39:07 1996
@@ -6,7 +6,7 @@
 
 CC	= gcc
 CFLAGS	= -O2 -fno-strength-reduce -Wall
-LFLAGS	= -lqt
+LFLAGS	= -lqt -lX11
 
 all: moc library tutorial examples
 
Only in osf1-gcc-static: Makefile.orig
diff -uwr template/library/Makefile osf1-gcc-static/library/Makefile
--- template/library/Makefile	Thu Aug 22 20:31:45 1996
+++ osf1-gcc-static/library/Makefile	Fri Aug 23 07:39:07 1996
@@ -76,7 +76,7 @@
 .SUFFIXES: .cpp
 
 .cpp.o:
-	$(CC) -I$(INCDIR) -c $(CFLAGS) -fPIC $<
+	$(CC) -I$(INCDIR) -c $(CFLAGS) $<
 
 # VERSION - the version number of the shared library, where applicable
 
@@ -85,14 +85,9 @@
 # Creates the library when all files have been compiled
 
 library: $(OBJECTS) $(METAOBJ)
-	-rm -f ../../../lib/libqt.so.${VERSION}
-	$(CC) -shared -Wl,-soname,libqt.so.0 \
-		-o ../../../lib/libqt.so.$(VERSION) \
-		$(OBJECTS) $(METAOBJ) -lX11
-	-rm -f ../../../lib/libqt.so
-	-ln -sf libqt.so.$(VERSION) ../../../lib/libqt.so
-	-rm -f ../../../lib/libqt.so.0
-	-ln -s libqt.so.$(VERSION) ../../../lib/libqt.so.0
+	ar q libqt.a $(OBJECTS) $(METAOBJ)
+	ranlib libqt.a
+	mv libqt.a ../../../lib
 
 depend:
 	makedepend $(SOURCES) 2> /dev/null
Only in osf1-gcc-static/library: Makefile.orig
