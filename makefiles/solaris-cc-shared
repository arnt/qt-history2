diff -ubr template/Makefile solaris-cc-shared/Makefile
--- template/Makefile	Sat Oct  5 23:42:52 1996
+++ solaris-cc-shared/Makefile	Sat Oct  5 23:42:51 1996
@@ -4,8 +4,8 @@
 # Read PORTING for instructions how to port Qt to a new platform.
 #
 
-CC	= gcc
-CFLAGS	= -O2 -fno-strength-reduce -Wall -W
+CC	= CC
+CFLAGS	= -O2 -pto
 LFLAGS	= -lqt
 SHELL	= /bin/sh
 
diff -ubr template/library/Makefile solaris-cc-shared/library/Makefile
--- template/library/Makefile	Sat Oct  5 23:40:17 1996
+++ solaris-cc-shared/library/Makefile	Sat Oct  5 23:40:35 1996
@@ -76,7 +76,7 @@
 .SUFFIXES: .cpp
 
 .cpp.o:
-	$(CC) -I$(INCDIR) -c $(CFLAGS) -fpic $<
+	$(CC) -I$(INCDIR) -c $(CFLAGS) -pic $<
 
 # VERSION - the version number of the shared library, where applicable
 
@@ -86,9 +86,8 @@
 
 library: $(OBJECTS) $(METAOBJ)
 	-rm -f ../lib/libqt.so.$(VERSION)
-	$(CC) -shared -Wl,-soname,libqt.so.1 \
-		-o ../lib/libqt.so.$(VERSION) \
-		$(OBJECTS) $(METAOBJ) -lX11
+	$(CC) -G -o ../lib/libqt.so.$(VERSION) \
+		$(OBJECTS) $(METAOBJ) -lX11 -lsocket
 	-rm -f ../lib/libqt.so
 	-ln -sf libqt.so.$(VERSION) ../lib/libqt.so
 	-rm -f ../lib/libqt.so.1
