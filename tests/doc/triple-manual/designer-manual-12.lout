@Chapter
    @Title { Reference: Toolbar Buttons }
    @Tag { reference.toolbar.buttons }
@Begin
@BeginSections
@Section
    @Title { Introduction }
    @Tag { introductionxxx }
@Begin
@LP
@QDR's toolbar buttons provide fast access to common
functionality. Toolbar buttons are grouped in several toolbars.
Toolbars have a handle at the left hand side which can be clicked
to minimize the toolbar. Toolbars that have been minimized have
their handle appear just under the menu bar; click the handle
to restore the toolbar to the last position it occupied. You
can drag a toolbar's handle to move the toolbar to a different
position in the toolbar area. Toolbars can be dragged out of
the toolbar area entirely and made into stand-alone tool dock
windows. To hide a tool dock window click its close button. To
restore a hidden tool dock window, right click the tool area,
then click the name of the tool dock window you wish to restore.

@End @Section
@Section
    @Title { The File Toolbar Buttons }
    @Tag { the.file.toolbar.buttons }
@Begin
@Figure
    @Caption { @I{File Toolbuttons} }
 @IncludeGraphic { "images/rtb-file.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{New}}{

 Click this toolbar button (or press @Option{Ctrl+N}) to create a new project, form or file. This option invokes the New File Dialog.}
@TagItem{
@Option{Open}}{

 Click this toolbar button (or press @Option{Ctrl+O}) to open existing projects, forms or files. This button invokes the File Open Dialog which is used to select files.}
@TagItem{
@Option{Save}}{

 Click this toolbar button (or press @Option{Ctrl+S}) to save the project, forms and files. For a new project that has no forms or files, click 'Save' to save the project before exiting. For new forms, click 'Save' and the Save Form As Dialog appears.}
@TagItem{
@Option{Active Project}}{

 Click the combobox to view the names of the projects that are currently open and select a project name to toggle between the projects.}
@EndList
@End @Section
@Section
    @Title { The Edit Toolbar Buttons }
    @Tag { the.edit.toolbar.buttons }
@Begin
@Figure
    @Caption { @I{Edit Toolbuttons} }
 @IncludeGraphic { "images/rtb-edit.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{Undo}}{

 Click this toolbar button (or press @Option{Ctrl+Z}) to undo an action. The name of the last action that was performed appears after the word 'Undo' in this toolbar button's tooltip.}
@TagItem{
@Option{Redo}}{

 Click this toolbar button (or press @Option{Ctrl+Y}) to redo an action. The name of the last action that was performed appears after the word 'Redo' in this toolbar button's tooltip.}
@TagItem{
@Option{Cut}}{

 Click this toolbar button (or press @Option{Ctrl+X}) to delete the selected item from the current form or file and copy it to the clipboard.}
@TagItem{
@Option{Copy}}{

 Click this toolbar button (or press @Option{Ctrl+C}) to copy the selected item from the current form or file to the clipboard.}
@TagItem{
@Option{Paste}}{

 Click this toolbar button (or press @Option{Ctrl+V}) to paste the selected item (if any) from the clipboard into the current form or file.}
@EndList
@End @Section
@Section
    @Title { The Search Toolbar Buttons }
    @Tag { the.search.toolbar.buttons }
@Begin
@Figure
    @Caption { @I{Search Toolbuttons} }
 @IncludeGraphic { "images/rtb-search.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{Find}}{

 Click this toolbar button (or press @Option{Ctrl+F}) to invoke the Find Text Dialog.}
@TagItem{
@Option{Find @LLP Incremental}}{

 Click this toolbar button (or press @Option{Alt+I}) to place the cursor in the text box located next to the @Option{Find} toolbar button. Type characters into the text box; as you type, @QDR will highlight the first occurrence of the text that it finds in the file. Press the @Option{Enter} key to go to the next occurrence of the text. Press the @Option{Esc} key once you have found the word you are looking for to place the cursor in the editor.}
@EndList
@End @Section
@Section
    @Title { The Tools Toolbar Buttons }
    @Tag { the.tools.toolbar.buttons }
@Begin
@LD
@Tbl
    strut { no }
    #rule { yes } rulecolour { red }
    aformat { @Cell marginleft { 0c } marginright { 0.5c } A | @Cell B }
{
    @Rowa A {
The tools toolbar buttons are available through toolbars (as
they have always been prior to Qt 3.1), and also via the new
Toolbox. From Qt 3.1, the tools toolbars are hidden and the Toolbox
is shown. The Toolbox is divided into groups, with the first
group being "\""Common Widgets"\"" which contains the widgets you're
likely to use most often. If you prefer to use the original toolbar
setup, you can access the toolbars by clicking @Option{Windows"|"Toolbars}
and then selecting the toolbars you want to be visible from the
context menu.
@SidebarPP
The Toolbox contains categories describing the type of toolbuttons
you can find within each catgory. Click a category name to access
the toolbuttons in that category. To use a widget in a category,
click the widget to activate it. Click the 'x' in the right hand
corner of the Toolbox to close it. To make the Toolbox appear
again, click @Option{Window"|"Views"|"Toolbox}.
@SidebarPP
If you want to add the same kind of widget several times to a
form, for example, several push buttons, @I{double click} the
widget's toolbar button. After this, each time you click the
form a new widget will be added. Click the @Option{Pointer} toolbar
button to leave this mode.
}
    B {
@Figure
    @Caption { @I{The Toolbox} }
    @Location { Raw }
 0.35 @Scale @IncludeGraphic { "images/rtb-toolbox.eps.gz" }
    }
}
@BeginSubSections
@SubSection
    @Title { Tools }
    @Tag{tools}
@Begin
@Figure
    @Caption { @I{Tools} }
 @IncludeGraphic { "images/rtb-tools.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{Pointer}}{

 Click this toolbar button (or press @Option{F2}) to de-select any selected widget toolbar button. The pointer is also used to stop inserting new widgets if you double clicked a widget toolbar button. Press the @Option{Esc} key to return to the pointer at any time.}
@TagItem{
@Option{Connect @LLP Signals and @LLP Slots}}{

 Click this toolbar button (or press @Option{F3}) to connect signals and slots. Then click on a widget and drag the connection line to the widget (or the form) that you want to connect to. Release the mouse button and the View and Edit Connections Dialog will appear.}
@TagItem{
@Option{Tab Order}}{

 Click this toolbar button (or press @Option{F4}) to set the tab order for all the widgets on the form that can accept keyboard focus. Click this toolbar button and blue circles with numbers on them appear next to the widgets. Click the widget that you want to be first in the tab order, then click the widget that should be next in the tab order, and continue until all the widgets have the tab order numbers you want. If you make a mistake, double click the first widget and start again. Press @Option{Esc} to leave tab order mode. If you want to revert your changes, leave tab order mode, then undo.}
@TagItem{
@Option{Set Buddy}}{

 Click this toolbar button (or press @Option{F5}) to set a buddy to a label. Then click on a label and drag the line to the widget that you want to be the buddy. Release the mouse button and the buddy is set.}
@EndList
@End @SubSection
@SubSection
    @Title { Buttons }
    @Tag{buttons}
@Begin
@Figure
    @Caption { @I{Buttons} }
 0.35 @Scale @IncludeGraphic { "images/rtb-tbbuttons.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{PushButton}}{

 Click this toolbar button, then click the form, to place a Pushbutton on the form.}
@TagItem{
@Option{ToolButton}}{

 Click this toolbar button, then click the form, to place a Toolbutton on the form.}
@TagItem{
@Option{RadioButton}}{

 Click this toolbar button, then click the form, to place a Radiobutton on the form. It is recommended that you place RadioButtons inside ButtonGroups so that Qt will automatically ensure that only one RadioButton in the group is active at any one time.}
@TagItem{
@Option{CheckBox}}{

 Click this toolbar button, then click the form, to place a CheckBox on the form.}
@EndList
@End @SubSection
@SubSection
    @Title { Containers }
    @Tag{containers}
@Begin
@Figure
    @Caption { @I{Containers} }
 0.35 @Scale @IncludeGraphic { "images/rtb-tbcontain.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{GroupBox}}{

 Click this toolbar button, then click the form, to place a GroupBox on the form.}
@TagItem{
@Option{ButtonGroup}}{

 Click this toolbar button, then click the form, to place a ButtonGroup on the form.}
@TagItem{
@Option{Frame}}{

 Click this toolbar button, then click the form, to place a Frame on the form.}
@TagItem{
@Option{TabWidget}}{

 Click this toolbar button, then click the form, to place a TabWidget on the form. To add or remove tabs, right click the tab widget and choose 'Add Page' or 'Remove Page'.}
@EndList
@End @SubSection
@SubSection
    @Title { Views }
    @Tag{views}
@Begin
@Figure
    @Caption { @I{Views} }
 0.35 @Scale @IncludeGraphic { "images/rtb-tbviews.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{ListBox}}{

 Click this toolbar button, then click the form, to place a ListBox on the form.}
@TagItem{
@Option{ListView}}{

 Click this toolbar button, then click the form, to place a ListView on the form.}
@TagItem{
@Option{Icon View}}{

 Click this toolbar button, then click the form, to place an IconView on the form.}
@TagItem{
@Option{Table}}{

 Click this toolbar button, then click the form to place a Table on the form.}
@EndList
@End @SubSection
@SubSection
    @Title { Database }
    @Tag{database}
@Begin
@Figure
    @Caption { @I{Database Toolbuttons} }
 0.35 @Scale @IncludeGraphic { "images/rtb-tbdatabase.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{DataTable}}{

 Click this toolbar button, then click the form, to place a DataTable on the form.}
@TagItem{
@Option{DataBrowser}}{

 Click this toolbar button, then click the form, to place a DataBrowser on the form.}
@TagItem{
@Option{DataView}}{

 Click this toolbar button, then click the form, to place a DataView on the form.}
@EndList
@End @SubSection
@SubSection
    @Title { Input }
    @Tag{input}
@Begin
@Figure
    @Caption { @I{Input Toolbuttons} }
 0.35 @Scale @IncludeGraphic { "images/rtb-tbinput.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{LineEdit}}{

 Click this toolbar button, then click the form, to place a LineEdit on the form.}
@TagItem{
@Option{SpinBox}}{

 Click this toolbar button, then click the form, to place a SpinBox on the form.}
@TagItem{
@Option{DateEdit}}{

 Click this toolbar button, then click the form, to place a DateEdit on the form.}
@TagItem{
@Option{TimeEdit}}{

 Click this toolbar button, then click the form, to place a TimeEdit on the form.}
@TagItem{
@Option{DateTimeEdit}}{

 Click this toolbar button, then click the form, to place a DateTimeEdit on the form.}
@TagItem{
@Option{TextEdit}}{

 Click this toolbar button, then click the form, to place a TextEdit on the form.}
@TagItem{
@Option{ComboBox}}{

 Click this toolbar button, then click the form, to place a ComboBox on the form.}
@TagItem{
@Option{Slider}}{

 Click this toolbar button, then click the form, to place a Slider on the form.}
@TagItem{
@Option{ScrollBar}}{

 Click this toolbar button, then click the form, to place a Scrollbar on the form.}
@TagItem{
@Option{Dial}}{

 Click this toolbar button, then click the form, to place a Dial on the form.}
@EndList
@End @SubSection
@SubSection
    @Title { Display }
    @Tag{display}
@Begin
@Figure
    @Caption { @I{Display Toolbuttons} }
 0.35 @Scale @IncludeGraphic { "images/rtb-tbdisplay.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{TextLabel}}{

 Click this toolbar button, then click the form, to place a TextLabel on the form.}
@TagItem{
@Option{PixmapLabel}}{

 Click this toolbar button, then click the form, to place a PixmapLabel on the form.}
@TagItem{
@Option{LCDNumber}}{

 Click this toolbar button, then click the form, to place a LCDNumber on the form.}
@TagItem{
@Option{Line}}{

 Click this toolbar button, then click the form, to place a Line on the form.}
@TagItem{
@Option{ProgressBar}}{

 Click this toolbar button, then click the form, to place a ProgressBar on the form.}
@TagItem{
@Option{TextBrowser}}{

 Click this toolbar button, then click the form, to place a TextBrowser on the form.}
@EndList
@End @SubSection
@SubSection
    @Title { Custom }
    @Tag{custom}
@Begin
@Figure
    @Caption { @I{Custom Widget Toolbutton} }
 0.35 @Scale @IncludeGraphic { "images/rtb-tbcustom.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{My Custom @LLP Widget}}{

 Click this toolbar button, then click the form, to place a Custom Widget on the form. Note: this toolbar button only appears if you have created a custom widget using @Option{Tools"|"Custom"|"Edit Custom Widgets}.}
@EndList
@End @SubSection
@EndSubSections
@End @Section
@Section
    @Title {The Layout Toolbar Buttons }
    @Tag{the.layout.toolbar.buttons}
@Begin
@Figure
    @Caption { @I{Layout Toolbuttons} }
 @IncludeGraphic { "images/rtb-layout.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{Adjust Size}}{

 Click this toolbar button (or press @Option{Ctrl+J}) to adjust the size of the widget to it's recommended size.}
@TagItem{
@Option{Lay Out @LLP Horizontally}}{

 Click this toolbar button (or press @Option{Ctrl+H}) to lay out the selected widgets or layouts side by side. Use @Option{Shift+Click} to select each widget or layout, and then choose this toolbar button to group them horizontally. Note that for complex widgets it is sometimes easiest to select widgets and layouts by clicking them in the Widgets tab of the @Win{Object Explorer} Window.}
@TagItem{
@Option{Lay Out @LLP Vertically}}{

 Click this toolbar button (or press @Option{Ctrl+L}) to lay out the selected widgets one above the other. Use @Option{Shift+Click} to select each widget or layout, and then choose this toolbar button to group them vertically. Note that for complex widgets it is sometimes easiest to select widgets and layouts by clicking them in the Widgets tab of the @Win{Object Explorer} Window.}
@TagItem{
@Option{Lay out in a @LLP Grid}}{

 Click the widgets you want and then click this toolbar button (or press @Option{Ctrl+G}) to lay out widgets in a grid.}
@TagItem{
@Option{Lay Out @LLP Horizontally @LLP (in Splitter)}}{

 Click this toolbar button to lay out the selected groups of widgets or layouts side by side with a splitter between each group. Use @Option{Shift+Click} to select each widget or layout, and then choose this toolbar button to group them horizontally. Note that for complex widgets it is sometimes easiest to select widgets and layouts by clicking them in the Widgets tab of the @Win{Object Explorer} Window.}
@TagItem{
@Option{Lay Out @LLP Vertically @LLP (in Splitter)}}{

 Click this toolbar button to lay out the selected groups of widgets or layouts one above the other with a splitter between each group. Use @Option{Shift+Click} to select each widget or layout, and then choose this toolbar button to group them vertically. Note that for complex widgets it is sometimes easiest to select widgets and layouts by clicking them in the Widgets tab of the @Win{Object Explorer} Window.}
@TagItem{
@Option{Break Layout}}{

 Click this toolbar button (or press @Option{Ctrl+B}) to break a layout. Click on the layout and select this option; the layout is deleted.}
@TagItem{
@Option{Add Spacer}}{

 Click this toolbar button to add a spacer to widgets that take up too much space on the form. The spacer consumes extra space in the layout.}
@EndList
@NP
@End @Section
@Section
    @Title { The Help Toolbar Button }
    @Tag { the.help.toolbar.button }
@Begin
@Figure
    @Caption { @I{Help Toolbutton} }
 @IncludeGraphic { "images/qd-helptoolbuttons3.eps.gz" }
@TaggedList labelwidth { 6f }
@TagItem{
@Option{What's This?}}{

 Click this menu option to invoke a small question mark that is attached to the mouse pointer. Click on a feature which you would like more information about. A message box appears with information about the feature.}
@EndList
@End @Section
@EndSections
@End @Chapter
