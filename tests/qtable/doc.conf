# -*- perl -*-
die '$QTDIR is not defined, stopped' unless (defined($ENV{"QTDIR"}));
die $ENV{"QTDIR"} . " is not a directory, stopped" unless (-d $ENV{"QTDIR"});

$sourcedir = $ENV{"QTDIR"} . "/tests/qtable";
@sourcedirs = ( $ENV{"QTDIR"} . "/tests/qtable" );
$docdir = $ENV{"QTDIR"} . "/tests/qtable/doc";
@docdirs = ( $ENV{"QTDIR"} . "/tests/qtable/doc" );
$outputdir = "" . $ENV{"QTDIR"} . "/tests/qtable/doc/html";
$includedir = $sourcedir;
@includedir = @sourcedir;

$maxerr = 640;

$module = "QTable";
$module_long = "Qt Table Widget";
$company = "Trolltech";

$postheader = "";

$footer = "";

$address = "";


$keyfile = $ENV{"QTDIR"} . "/tests/qdable//doc/.qdoc-links";

$style = "<style type=\"text/css\"><!--\nh3.fn,span.fn { margin-left: 1cm; text-indent: -1cm; }\na:link { text-decoration: none; }\nbody { background: white; color: black; }\n--></style>";

# $debug = 1;

sub post {
}
