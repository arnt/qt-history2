/*! \page qaxserver-demo-simple.html

  \title A standard ActiveX and the "simple" ActiveQt widget.

\raw html
<object ID="QSimpleAX" CLASSID="CLSID:DF16845C-92CD-4AAB-A982-EB9840E74669"
CODEBASE=http://www.trolltech.com/demos/simpleax.cab>
    <PARAM NAME="text" VALUE="A simple control">
    <PARAM NAME="value" VALUE="1">
[Object not available! Did you forget to build and register the server?]
</object>

<form ID="form">
    <INPUT TYPE="BUTTON" VALUE="About..." onClick="QSimpleAX.about()"><br>
    <INPUT TYPE="EDIT" ID="TextEdit" VALUE="Enter Text" onKeyUp="setText()"><br>
    <INPUT TYPE="EDIT" ID="NumberEdit" VALUE="Enter Number" onKeyUp="setValue()"><br>
</form>

<SCRIPT LANGUAGE=JavaScript>
function setText()
{
    QSimpleAX.text = form.TextEdit.value
}

function setValue()
{
    QSimpleAX.value = form.NumberEdit.value
}
</SCRIPT>
\endraw
*/

/*! \page qaxserver-example-simple.html
    \ingroup qaxserver-examples
  \title A simple ActiveQt control (executable)


  The ActiveX control in this example is a simple QWidget subclass
  displaying some data. It provides a signal/slot/property interface 
  to change the data and to send notifications of property changes,
  and uses the default QAxFactory through the QAXFACTORY_DEFAULT macro.

  \quotefile simple/main.cpp

  The Qt implementation of QWidget providing the ActiveX control for 
  this example is straight forward:
  \skipto class QSimpleAX
  \printto #include
  \skipto QAXFACTORY

  \printto )
  The control is exported using the default QAxFactory.

  Note that even though the example is an out-of-process server
  there is is no implementation of a \c main function. This is because
  ActiveQt provides a default implementation of \c main that is used if
  the application itself doesn't provide one.

  To build the example you must first build the \link qaxserver.html
  QAxServer \endlink library. Then run qmake and your make tool in 
  \c examples/simple.

  <hr>

  The \link qaxserver-demo-simple.html demonstration \endlink requires your
  WebBrowser to support ActiveX controls, and scripting to be enabled.

  \quotefile simple/simple.doc
  \skipto <object ID
  \printuntil </object>
  The simple ActiveX control is embedded using the \c <object> tag.

  \printuntil </FORM>
  A simple HTML form is provided to change the properties of the ActiveX.

  \skipto VBScript
  \printuntil </SCRIPT>
  \printuntil </SCRIPT>

  Events from the ActiveX controls are handled using both Visual Basic Script
  and JavaScript.
*/
