reimplement keyPressEvent i QMessageBox og fjern ugle hack i
QDialog::keyPressEvent

	/*
	  Todo!!! We can have a function in qcol_x11, qt_xcolortable,
	  which returns carr and does not delete it in the same events
	  batch.
	 */


	// THIS IS NOT SUFFICIENT. SCAN FOR START AND
	// STOP VALUES !!!

QImage::systemByteOrder ska'n vekk, elle?

Klin inn kule sider mellom tutorial og doc i ps. (signal/slot, debugging, etc.)

Alle changes filene paa web

Spacing er borte i ps versjonen av tutorialen

oooops 3 reserverte ord i qt: signals, slots, emit

	debug, warning, fatal

	ASSERT

	chk_pointer

	CHECK_PTR

	INT8, UINT8, ...

	OBSOLETE

TEST_OBSOLETE

***** flyttet fra qt/src:

1) SetBackgroundColor, setPalette, setFont bør kunne ha
   kaskade-setting.

2) QPoint, QSize, QRect double-funksjoner, avrund, ikke trunker.

3) QFont.  Skal sette Helvetica hvis en raw-font endres.

4) Operatorer rett etter {c|d}-tor.

5) QComboBox og QListBox må gjøre riktig bruk av musknapper.

6) QFile::open()-feil -> resett open-flagg.

7) QListBox::insertItem(), inSort() + evt. QComboBox skal returnere int.

8) qpshdr.cpp istedet for qtheader.cpp.

9) Legg til void QTableView::setCellSize() og evt. QSize cellSize().

10) QPainter::drawShade... skal ha bool invert-flagg.

10) sentrer pixmaps i QComboBox
